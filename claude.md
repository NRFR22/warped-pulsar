# Agent Guidelines (Claude)

- Automatically `git add` and `git commit` your changes when the task is finished.
- Use a clear commit message that explains the modifications.
- Verify that `git status` is clean before you finish, so the repo stays tidy for the next agent.
